[build]
  command = "npm run build"
  publish = "dist"

# Wajib untuk React Router: semua path di-redirect ke index.html
# supaya refresh di /transaksi atau /login gak dapat 404 dari Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
